export default function NotificationRules() {
  return (
    <div className="p-6 text-white">
      <h1 className="text-2xl font-bold mb-2">Notification rules</h1>
      <p className="text-gray-400">
        Schedule emails for time reminders or stock summaries. Coming online in Phase 4 — the
        backend, scheduler, and Resend integration will land together so the rules actually fire.
      </p>
    </div>
  );
}
